# Streamlit for UI
streamlit>=1.32.0

# LangChain for agent framework
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20

# Anthropic Claude integration for LangChain (PRIMARY LLM)
langchain-anthropic>=0.3.0
anthropic>=0.40.0

# OpenAI integration for Azure OpenAI and Bedrock compatibility
langchain-openai>=0.1.0
openai>=1.0.0

# Ollama integration for local LLM models (qwen3, llama3, etc.)
langchain-ollama>=0.2.0

# LangGraph for agent orchestration (dual orchestration system)
langgraph>=0.2.0

# MCP SDK for connecting to MCP servers
mcp>=0.9.0

# BioMCP for biomedical research tools
biomcp-python>=0.7.0

# HTTP client for MCP
httpx>=0.25.0

# Additional utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Persistent Context Layer (required for DB models even when feature is disabled)
sqlalchemy>=2.0.0
markdown>=3.5.0

# Persistent Context Layer - vector search (Required for Context Forge)
chromadb>=0.4.0
sentence-transformers>=2.2.0
numpy>=1.24.0

# Optional: PDF report export (requires system-level GTK/Cairo libraries)
weasyprint>=60.0

# Optional: Tavily web search integration
tavily-python>=0.3.0